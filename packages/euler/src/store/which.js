import { action, computed, makeAutoObservable, makeObservable, observable, get, toJS, remove } from "mobx";
import { notification } from 'antd';
import { observer } from 'mobx-react-lite';


export {
  //mobx-react-lite
  action,
  computed,
  makeObservable,
  makeAutoObservable,
  observable,
  get,
  //mobx-react
  observer,
  toJS,
  remove,
  //antd
  notification,
}


